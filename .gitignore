# Personal vault data — never commit (public repo)
data/

# Cached wishlist downloads (committed-or-not still TBD, see PLAN.md)
wishlists/

# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
build/
dist/

# WSL metadata
*:Zone.Identifier
